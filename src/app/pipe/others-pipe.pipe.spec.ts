import { OthersPipePipe } from './others-pipe.pipe';

describe('OthersPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OthersPipePipe();
    expect(pipe).toBeTruthy();
  });
});
